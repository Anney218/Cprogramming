//2 WAP that checks the length of a given string
#include<stdio.h>
#include<string.h>
int main(){
    char s1[]="hello";
    int len=strlen(s1);
    printf("length=%d\n",len);
    return 0;
}
